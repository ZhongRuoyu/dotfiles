# shellcheck shell=sh

# shellcheck source=/dev/null
. "$HOME/.shell/interactive"

# Prompt settings
PS1="[\u@\h \W]\\$ "

# Local interactive profile
if [ -r "$HOME/.sh/interactive.local" ]; then
  # shellcheck source=/dev/null
  . "$HOME/.sh/interactive.local"
fi
