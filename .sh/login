# shellcheck shell=sh

# shellcheck source=/dev/null
. "$HOME/.shell/login"

# Local login profile
if [ -r "$HOME/.sh/login.local" ]; then
  # shellcheck source=/dev/null
  . "$HOME/.sh/login.local"
fi
