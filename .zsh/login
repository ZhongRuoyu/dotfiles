# shellcheck disable=SC2148

# shellcheck source=/dev/null
source "$HOME/.shell/login"

# Local login profile
if [[ -e "$HOME/.zsh/login.local" ]]; then
  # shellcheck source=/dev/null
  source "$HOME/.zsh/login.local"
fi
