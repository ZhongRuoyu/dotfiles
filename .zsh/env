# shellcheck disable=SC2148

# shellcheck source=/dev/null
source "$HOME/.shell/env"

# Local env profile
if [[ -r "$HOME/.zsh/env.local" ]]; then
  # shellcheck source=/dev/null
  source "$HOME/.zsh/env.local"
fi
