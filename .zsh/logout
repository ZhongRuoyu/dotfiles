# shellcheck disable=SC2148

# shellcheck source=/dev/null
source "$HOME/.shell/logout"

# Local logout profile
if [[ -r "$HOME/.zsh/logout.local" ]]; then
  # shellcheck source=/dev/null
  . "$HOME/.zsh/logout.local"
fi
