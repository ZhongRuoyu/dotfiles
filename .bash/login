# shellcheck shell=bash

# shellcheck source=/dev/null
source "$HOME/.shell/login"

# Local login profile
if [[ -r "$HOME/.bash/login.local" ]]; then
  # shellcheck source=/dev/null
  source "$HOME/.bash/login.local"
fi
