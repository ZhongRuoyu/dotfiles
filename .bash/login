# shellcheck shell=bash

# shellcheck source=/dev/null
source "$HOME/.shell/login"

# Local login profile
if [[ -e "$HOME/.bash/login.local" ]]; then
  # shellcheck source=/dev/null
  . "$HOME/.bash/login.local"
fi
