# shellcheck shell=bash

# shellcheck source=/dev/null
source "$HOME/.shell/env"

# Local env profile
if [[ -r "$HOME/.bash/env.local" ]]; then
  # shellcheck source=/dev/null
  source "$HOME/.bash/env.local"
fi
