# shellcheck shell=bash

# shellcheck source=/dev/null
source "$HOME/.shell/logout"

# Local logout profile
if [[ -r "$HOME/.bash/logout.local" ]]; then
  # shellcheck source=/dev/null
  . "$HOME/.bash/logout.local"
fi
